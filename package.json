{
  "main": "ecosystem.config.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start ecosystem.config.js",
    "devinstall": "(npm install && cd Auth) & (npm install && cd ..) & (cd families && npm install && cd ..) & (cd documents && npm install && cd ..)"
  },
  "dependencies": {
    "nodemon": "^2.0.20",
    "pm2": "^5.2.2"
  },
  "devDependencies": {
  }
}
